{
  "name": "spiti-folio-archive",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Publish a manuscript archive to Swarm behind a stable feed address that outlives the app, the grant and the publisher.",
  "engines": { "node": ">=20.12" },
  "scripts": {
    "init": "tsx src/init.ts",
    "publish": "tsx src/publish.ts",
    "recover": "tsx src/recover.ts",
    "status": "tsx src/status.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ethersphere/bee-js": "13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
